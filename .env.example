# ── BradlyAI Environment Configuration ─────────────────────────────────
# Copy this file to .env and fill in your values.
#   cp .env.example .env

# Application
APP_NAME="BradlyAI - Driverless SOC & Automated Incident Response"
APP_VERSION="2.1.0-PRO"
ENVIRONMENT=development

# Database (SQLite is the default — no config needed)
DATABASE_URL=sqlite+aiosqlite:///./bradlyai_soc.db

# ── AI / LLM Configuration ────────────────────────────────────────────
# Choose your provider: 'groq' (Llama-3, free tier) or 'openai' (GPT-4)
LLM_PROVIDER=groq

# Groq API Key (get one free at https://console.groq.com)
GROQ_API_KEY=gsk_your_key_here

# OpenAI API Key (get one at https://platform.openai.com)
OPENAI_API_KEY=sk-your_key_here

# Default model for OpenAI
DEFAULT_AI_MODEL=gpt-4-turbo-preview

# ── Autonomous SOC Settings ────────────────────────────────────────────
# AI confidence threshold (0.0–1.0) for auto-containment
AUTO_CONTAINMENT_THRESHOLD=0.85

# Enable background live simulation worker
LIVE_SIMULATION_WORKER_ACTIVE=true

# How often the simulation generates new alerts (seconds)
SIMULATION_INTERVAL_SECONDS=30

# ── Rate Limiting ──────────────────────────────────────────────────────
RATE_LIMIT_ENABLED=true
RATE_LIMIT_REQUESTS=300
RATE_LIMIT_WINDOW_SECONDS=60

# ── Wazuh Manager API Integration ─────────────────────────────────────
# SAFETY: All defaults are SAFE for production. Read carefully before enabling.
#
# To enable (in this order):
#   1. Set WAZUH_ENABLED=true (off by default)
#   2. Set WAZUH_DRY_RUN=true initially (logs actions without executing)
#   3. Test with: curl /api/v1/l1/wazuh/health
#   4. When confident, set WAZUH_DRY_RUN=false
#   5. Choose close_mode:
#      - comment_only: SAFEST, just adds audit comment, doesn't archive
#      - archive_only: archives alert but no comment
#      - archive_and_comment: archives + adds comment (most informative)
#
WAZUH_ENABLED=false
WAZUH_DRY_RUN=true
WAZUH_CLOSE_MODE=comment_only
WAZUH_MANAGER_URL=
WAZUH_USER=
WAZUH_PASSWORD=
WAZUH_VERIFY_SSL=true
