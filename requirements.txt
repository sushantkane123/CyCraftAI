# ── Core ──
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
sqlalchemy>=2.0.28
pydantic>=2.6.4
pydantic-settings>=2.2.1
websockets>=12.0
python-multipart>=0.0.9
rich>=13.7.1

# ── Database drivers ──
aiosqlite>=0.20.0           # SQLite (default, dev/demo)
asyncpg>=0.29.0             # Postgres (production)
psycopg2-binary>=2.9.9      # Postgres (sync fallback)

# ── HTTP / Async ──
httpx>=0.27.0
requests>=2.31.0
slowapi>=0.1.9              # rate limiting

# ── Auth / Security ──
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0    # JWT
python-multipart>=0.0.9            # OAuth2 password flow form parsing
authlib>=1.3.0                     # OAuth2 / OIDC / SAML
pyotp>=2.9.0                       # MFA TOTP
qrcode[pil]>=7.4.2                 # MFA QR setup

# ── SIEM / EDR / Network / Identity clients ──
# (most use httpx above — explicit libs only where useful)
pyyaml>=6.0.1                      # Sigma / playbook YAML
jinja2>=3.1.3                      # notification templates

# ── Observability ──
prometheus-client>=0.20.0
prometheus-fastapi-instrumentator>=6.1.0
opentelemetry-api>=1.24.0
opentelemetry-sdk>=1.24.0
opentelemetry-instrumentation-fastapi>=0.45b0

# ── Reporting ──
reportlab>=4.1.0                   # PDF reports
openpyxl>=3.1.2                    # Excel exports

# ── Testing ──
pytest>=8.1.1
pytest-asyncio>=0.23.6
pytest-cov>=4.1.0
faker>=24.0.0
